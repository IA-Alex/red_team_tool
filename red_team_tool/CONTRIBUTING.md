# Contributing to Zotz

We welcome contributions! Please adhere to the following:

- **Conventional Commits:** Use clear, semantic commit messages (e.g., `feat: ...`, `fix: ...`, `docs: ...`).
- **Pre-commit Hooks:** Ensure `pre-commit` hooks pass before submitting a PR.
- **Testing:** New features MUST include unit or integration tests.
